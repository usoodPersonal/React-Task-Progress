App.jsx
│
├── Navbar (gets setNavbarTab)
│
└── Routes
    ├── "/"            → Overview.jsx
    ├── "/analysis"    → Analysis.jsx
    ├── "/policies"    → Policies.jsx
    ├── "/devices"     → Devices.jsx
    ├── "/objects"     → Objects.jsx  
    └── "/integration" → Integration.jsx



Objects.jsx
 ├─ ListProvider (Context)
 │    └─ provides { lists, addItemToPage, setLists }
 │
 ├─ Sidebar.jsx (menu navigation)
 │    └─ uses NavLink for active route highlighting
 │
 └─ Routes (Dynamic)
      └─ Page.jsx (for each object type)
           ├─ Reads/Writes to lists via Context
           ├─ Local UI State (modalOpen, formData, overrides, editingIndex)
           ├─ CRUD operations for objects
           └─ Override management



Page.jsx
 ├─ Reads `lists[pageName]` from ListContext
 ├─ Displays table of objects
 │    ├─ Edit → populate modal
 │    ├─ Delete → remove from context
 ├─ Add Object Modal
 │    ├─ Form for object data
 │    ├─ Override Section (expandable)
 │    │    ├─ Table of overrides
 │    │    ├─ Edit/Delete override
 │    │    └─ Add Override Modal
 │    └─ Save → updates ListContext
 └─ Overrides managed as part of object state

 


Page.jsx 

 {
  name: string,
  description: string,
  textbox: string,
  allowOverride: boolean,
  radioOption: "Host" | "Range" | "Network" | "FQDN",
  overrides: [
    {
      overrideOn: string,
      content: string,
      type: string
    },
    ...
  ]
}

